package main

func main() {
	var n int = 300000000
	for i := 0; i < n; i++ {
		tempInt := 0
		tempString := "Some string."
		var tempStruct struct{}
		var tempArr []bool // Use Bool since its smallest size
		
		if tempInt == 0 {
		} 
		if tempString == "Some string." {
		}
		if &tempStruct != nil {
		}
		if tempArr != nil {
		}
	}
}

